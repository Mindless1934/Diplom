@model IEnumerable<Branches>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BranchNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BranchNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idBranch }) |
            @Html.ActionLink("Details", "Details", new { id=item.idBranch }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idBranch })
        </td>
    </tr>
}

</table>
