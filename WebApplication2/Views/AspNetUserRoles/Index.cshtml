@model IEnumerable<AspNetUserRoles>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            Роль
        </th>
        <th>
            E-mail
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetRoles.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUsers.Email)
        </td>
        <td>
            @Html.ActionLink("Изменить", "Edit", new { id=item.UserRoleId }) |
            @*@Html.ActionLink("Детали", "Details", new { id=item.UserRoleId }) |*@
            @Html.ActionLink("Удалить", "Delete", new { id=item.UserRoleId })
        </td>
    </tr>
}

</table>
